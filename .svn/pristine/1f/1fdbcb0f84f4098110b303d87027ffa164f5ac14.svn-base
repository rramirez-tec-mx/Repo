#pragma once
#include <windows.h>
#include <string>

#define DLL_EXPORT __declspec(dllexport)

class DLL_EXPORT CryptCypherOperation
{
public:
	CryptCypherOperation(void);
	~CryptCypherOperation(void);

	BYTE* EncryptData(std::wstring cryptAlgType, const int lenPlainText, BYTE *plainText, ULONG & lenCypherText);
	BYTE* DecryptData(std::wstring cryptAlgType, const int lenCypherText, BYTE *cypherText);
};

